{"question": "How many customers are there?", "db_id": "shop", "gold_sql": "SELECT COUNT(*) FROM customers"}
{"question": "What is the name of the customer who placed the most orders?", "db_id": "shop", "gold_sql": "SELECT c.name FROM customers c JOIN orders o ON o.customer_id=c.id GROUP BY c.id ORDER BY COUNT(*) DESC LIMIT 1"}
{"question": "Which city has the most customers?", "db_id": "shop", "gold_sql": "SELECT city FROM customers GROUP BY city ORDER BY COUNT(*) DESC LIMIT 1"}
